import { DeckCreateContainer } from "./deck-create/deck-create.container";
import { DeckDetailContainer } from "./deck-detail/deck-detail.container";
import { DecksContainer } from "./decks/decks.container";

export {
    DeckCreateContainer, DeckDetailContainer, DecksContainer
}

export const containers = [
    DeckCreateContainer, DeckDetailContainer, DecksContainer
]
